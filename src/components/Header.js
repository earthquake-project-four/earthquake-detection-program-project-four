import logo from "../assets/ecs-logo.png"

const Header = () => {
    return (
        <header>
            <div className="logo">
                <img src={ logo } alt="esc logo" />
            </div>
        </header>
    )
};

export default Header;
